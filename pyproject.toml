[project]
name = "legal-domain-graphrag"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["boto3>=1.34", "pyyaml>=6.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
